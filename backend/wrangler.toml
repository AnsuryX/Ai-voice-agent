name = "qatar-real-estate-bot"
main = "main.py"
compatibility_date = "2024-04-01"
compatibility_flags = ["python_workers"]

[vars]
# These will be set as secrets using wrangler secret put
# WHATSAPP_TOKEN, WHATSAPP_PHONE_ID, WHATSAPP_VERIFY_TOKEN, GROQ_API_KEY, etc.
